fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.20.0
pydantic>=2.5.0
cryptography>=42.0.0
httpx>=0.27.0
openai>=1.12.0
anthropic>=0.18.0
python-multipart>=0.0.7
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0
python-telegram-bot>=21.0
ddgs>=9.0.0
bcrypt>=4.0.0
croniter>=1.3.0
# Imported directly by tasks/nl_time.py for NL task times ("tomorrow at 8").
# Was transitive-only — one resolver change from an ImportError at runtime.
python-dateutil>=2.8.2
# Used by mcp/db_toolbox.py to generate the db-toolbox tools.yaml. Was
# transitive-only (MIT). Pinned here so config generation can't ImportError.
PyYAML>=6.0
prompt_toolkit>=3.0.0
textual>=0.50.0
mcp>=1.0.0
n8n-mcp-server>=0.1.0
workspace-mcp>=1.19.0,<1.21.0
pytesseract>=0.3.10
pyahocorasick>=2.0.0
# Voice STT — Telegram voice notes + Web UI mic. Mirrors pyproject.toml base
# deps so `pip install -r requirements.txt` (Dockerfile layer-cache step)
# pre-fetches the wheels before the slow `pip install -e .` step.
pywhispercpp>=1.2.0
imageio-ffmpeg>=0.4.9
# Claude Agent SDK — MODE_CLAUDE "sdk" transport. Mirrored from
# pyproject.toml so Docker layer-cache picks it up before pip install -e.
claude-agent-sdk>=0.1.81
# sqlite-vec — vec0 virtual table for LazyBrain semantic search. Mirrored
# from pyproject.toml so Docker layer-cache picks it up before pip
# install -e. Graceful fallback when extension can't load.
sqlite-vec>=0.1.9
# Sheets feature — server-side .xlsx read/write + formula eval. Mirrored
# from pyproject.toml so the Docker layer-cache pre-fetches the wheels
# before `pip install -e .`. All MIT-licensed.
openpyxl>=3.1.0
python-calamine>=0.2.3
xlcalculator>=0.5.0
# Docs feature — .docx generation from the Univer document snapshot. MIT.
python-docx>=1.1.0
# PDF toolkit — pypdf (BSD-3) + reportlab (BSD) + pdfplumber (MIT) + pikepdf
# (MPL-2.0). Permissive only; deliberately NOT PyMuPDF/borb (AGPL).
pypdf>=4.0.0
reportlab>=4.0.0
pdfplumber>=0.11.0
pikepdf>=8.0.0
# Password-protected export — AES-256 encrypted .zip wrapper (MIT).
pyzipper>=0.3.6
